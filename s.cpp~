// ofstream constructor.
#include <fstream>      // std::ofstream
#include <iostream>
#include <vector>

using namespace std;
	vector<int> v;

int main () {

	cout << "v.size() = "<< v.size() << endl;
	return 0;
}
